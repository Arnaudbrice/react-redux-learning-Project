# features/todos/

Hier entsteht später unser `todoSlice.ts`.

Wir lernen dabei:

- `createSlice()`
- `initialState`
- Reducer
- Actions
- `PayloadAction<T>`

Die Datei wird in einem späteren Ticket gemeinsam erstellt.
