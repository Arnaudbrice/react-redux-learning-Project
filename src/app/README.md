# app/

Hier kommt später die allgemeine Redux-Konfiguration hinein.

## Ticket 1.1

Erstelle `store.ts` und lerne dabei:

- `configureStore()`
- warum Redux einen Store braucht
- wie ein Reducer in den Store registriert wird
- `RootState`
- `AppDispatch`

Noch nichts implementieren, bevor wir Ticket 1.1 gemeinsam besprochen haben.
